# .nosana-ci.yml
nosana:
  description: Build, Test and Deploy software

global:
  image: node:16
  trigger:
    push:
      branches:
        - main

jobs:
  - name: build
    commands:
      - npm ci
      - npx hardhat compile
  - name: test
    commands:
      - echo 'test.'
