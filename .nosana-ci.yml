# .nosana-ci.yml
nosana:
  description: Build, Test and Deploy software

global:
  image: node:16
  trigger:
    push:
      branches:
        - main

jobs:
  - name: build
    commands:
      - echo $SECRET_THAT_DOESNT_EXISTS
